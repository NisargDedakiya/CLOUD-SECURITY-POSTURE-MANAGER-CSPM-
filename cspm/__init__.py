"""Cloud Security Posture Manager (CSPM).

A multi-cloud (AWS, GCP, Azure) security posture scanner. Providers collect
normalized resources, a set of Python-coded checks evaluate them, and the
engine aggregates the results into a scan report served over a REST API and
a web dashboard.
"""

__version__ = "0.1.0"
